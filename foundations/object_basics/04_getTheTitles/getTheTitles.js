const getTheTitles = function(arr) {
  let empty = [];
  
  let title = arr.map((book) => {
    return book.title;
    
  })

  empty.push(title);

  return (title);
  

};
// Do not edit below this line
module.exports = getTheTitles;
